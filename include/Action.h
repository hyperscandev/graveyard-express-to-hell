#ifndef ACTION_H
#define ACTION_H

/**
 * Action class
 *
 * base class that handles the different possible actions
 */
class Action {
	public:
		//! pure virtual destructor
		virtual ~Action() = 0;
};
#endif
